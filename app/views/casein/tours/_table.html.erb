<!-- Scaffolding generated by Casein v5.3.1.0 -->

<table class="table table-striped table-hover table-condensed">
  <thead>
    <tr>
      <th><%= casein_sort_link "Name", :name %></th>
      <th><%= casein_sort_link "Description", :description %></th>
      <th>&nbsp;</th>
    </tr>
  </thead>

  <% tours.each do |tours| %>
    <tr>
      <td><%= casein_table_cell_link tours.name, casein_tours_path(tours) %></td>
      <td><%= casein_table_cell_link tours.description, casein_tours_path(tours) %></td>
      <td class="delete">
        <%= link_to(casein_show_row_icon("trash"), casein_tours_path(tours), method: :delete, data: { confirm: "Are you sure you want to delete this tours?" }) %>
      </td>
    </tr>
  <% end %>
</table>
